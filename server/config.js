/**
 * Payment Configuration Constants
 */

// Supported currencies for PayPal payments
const SUPPORTED_CURRENCIES = ['USD', 'EUR', 'GBP', 'CAD', 'AUD', 'JPY'];

module.exports = {
  SUPPORTED_CURRENCIES
};
